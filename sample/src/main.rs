use std::collections::HashMap;

fn main(){
    // let mut hm = String::new();
    // hm.push_str("Cucumber,Vegetable,Banana,Fruit,Carrot,Vegetable,Apple,Fruit,Chicken,Meat");
    // let v = hm.split(',');
    // let mut vec = Vec::new();
    // for i in v{vec.push(i)}
    // println!("{:?}", vec[::5])
    println!("{:?}", 0%2==0)
}